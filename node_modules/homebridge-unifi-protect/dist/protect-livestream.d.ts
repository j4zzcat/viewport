import { ProtectCamera } from "./devices/index.js";
import { ProtectLivestream } from "unifi-protect";
export declare class LivestreamManager {
    private livestreams;
    private protectCamera;
    private subscriberCount;
    constructor(protectCamera: ProtectCamera);
    private getIndex;
    acquire(channel: number, lens?: number): ProtectLivestream;
    start(channel: number, lens?: number, segmentLength?: number): Promise<boolean>;
    stop(channel: number, lens?: number): void;
}
