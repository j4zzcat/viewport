SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
source "$SCRIPT_DIR"/../src/viewport/.venv/bin/activate

PYTHONUNBUFFERED=1 \
PYTHONWARNINGS="ignore:Unverified HTTPS request" \
python "$SCRIPT_DIR"/../src/viewport/src/cli/cli.py $@
